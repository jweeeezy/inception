
# run all containers in detached mode
# docker compose up -d 

# stop all containers and remove them
# docker compose down

# use docker logs for logfiles (mb redirect them into sep files?)
# docker logs <container_name>
